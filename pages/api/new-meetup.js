//api/new-meetup
//POST api/new-mmetup
function handler(req, res) {
  if (req.mwthod === "POST") {
    const data = req.body;

    const { title, image, address, description } = data


    
  }
}

export default handler;
